/* Compatibility translation unit. The reusable implementation lives in
 * lib/recomp_gamepad and remains source-compatible with existing PSX builds. */
#include "../../lib/recomp_gamepad/src/controller_identity.cpp"
