[package]
name = "gba-mvii"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "C ABI + bare-metal runtime floor binding gba-core into an MVII Virtua executable"

[lib]
name = "gba_mvii"
crate-type = ["staticlib"]

[dependencies]
gba-core = { path = "../gba-core" }
armv4t = { path = "../armv4t" }
