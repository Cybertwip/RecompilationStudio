[package]
name = "native-gamepad"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Optional native controller backends shared with recomp C++ runtimes"
build = "build.rs"

[build-dependencies]
cc = "1"
pkg-config = "0.3"
